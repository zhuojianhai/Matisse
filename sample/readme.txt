git merge --continue

合并，并不是真的合并，而是会生成一个新的commit


git  add .
git checkout feature

git reset bcsddxx
git  clone
合并代码步骤
1、切换到需要合并的分支
2、使用git merge 命令，后面跟着需要从哪个分支合并的名称
3、处理冲突
4、git add .
5、git merge --continue
